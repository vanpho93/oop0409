const arrNum = [1, 3, 7, 8, 2];

const arrPerson = [
    { name: 'Teo', age: 10 },
    { name: 'Ti', age: 12 },
    { name: 'Tun', age: 14 }
];

//Person Company

// console.log(arrNum.map(e => console.log(e)));
console.log(arrPerson.map(person => person.age));
// console.log(arrNum.map(person => person.name));
